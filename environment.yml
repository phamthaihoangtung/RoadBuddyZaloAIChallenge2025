name: road-buddy
channels:
  - pytorch
  - nvidia
  - xformers
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip
  
  # Core dependencies
  - numpy
  - pandas
  - matplotlib
  - pyyaml
  - tqdm
  - av

  # Additional dependencies via pip
  - pip:
      - python-dotenv>=1.2.1
      - wandb
      - trackio
      - vllm
      - qwen_vl_utils

